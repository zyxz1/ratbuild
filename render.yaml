services:
  - type: web
    name: dualhook-builder
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: MASTER_TOKEN
        sync: false
